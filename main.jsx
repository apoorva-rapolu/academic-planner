import React from 'react';
import ReactDOM from 'react-dom/client';
import AcademicCalendar from './academic-calendar.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AcademicCalendar />
  </React.StrictMode>
);
